{
  "name": "@flight/web",
  "version": "1.0.0",
  "description": "TypeScript web frontend SPA",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist",
    "test": "echo 'Test script not configured yet'"
  },
  "devDependencies": {
    "@types/node": "^16.18.0",
    "esbuild": "^0.19.12",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "16.x"
  }
}
